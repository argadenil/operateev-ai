import { notFound } from "next/navigation";

// This route has been removed. Preserve the file to avoid breaking imports, but return 404.
export default function RemovedProfileByIdPage() {
  notFound();
}
